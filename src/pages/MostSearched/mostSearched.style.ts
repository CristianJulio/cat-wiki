import styled from "styled-components";

export const MostSearchedWrapepr = styled.div`
  margin-bottom: 106px;
`
export const Title = styled.h2`
  color: var(--clr-caffe);
  font-family: var(--ff-montserrat);
  font-size: 2.25rem;
  font-weight: var(--fw-700);
  line-height: 38px;
  margin-bottom: 52px;
  margin-top: 40px;
`
export const ItemsWrapper = styled.div`
  display: grid;
  grid-gap: 55px;
`